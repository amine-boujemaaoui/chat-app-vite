// controllers/messageController.ts

export async function getMessages(req, res) {
  // Implémentez la logique pour récupérer les messages
  res.status(200).json({ message: "getMessages non implémenté" });
}

export async function sendMessage(req, res) {
  // Implémentez la logique pour envoyer un message
  res.status(200).json({ message: "sendMessage non implémenté" });
}
